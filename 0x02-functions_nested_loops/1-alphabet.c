#include "main.h"
/**
 * main - Entry point
 *
 * Return: always 0
 *
 * print allphabets in lowercase
 */

void print_alphabet(void)

{
	char alpha;
	
	for (alpha = 'a'; alpha <= 'z'; alpha++ );
	_putchar(alpha);
	_putchar('\n');
}
